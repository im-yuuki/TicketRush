services:
  ticketrush:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    images: ticketrush:latest
    container_name: ticketrush
    ports:
      - "8080:80/tcp"
    restart: unless-stopped
